# AI Agent Rules — <%= projectName %>

> Generated by [007devkit](https://github.com/007devkit/007devkit) on <%= date %>
> Stack: <%= hasFrontend ? frontend : '' %><%= hasBackend ? (hasFrontend ? ' + ' : '') + backend : '' %><%= hasMobile ? ' + ' + mobile : '' %><%= hasDatabase ? ' + ' + database : '' %><%= hasMonorepo ? ' (Nx monorepo)' : '' %>

---

## 1. Mandatory Reading Before Generating Code

Before creating or modifying any file in this project, **you MUST read**:

| Document | Path | Applies to |
|----------|------|------------|
| This file | `.cursorrules` | Everything |
<% if (hasBackend && isNestjs) { %>| NestJS Standards | `docs/nest-backend-general-standards.md` | `<%= isNx ? 'apps/api' : 'src' %>` |
<% } %><% if (hasFrontend && (isAngular || isReact || isNextjs || isVue || isNuxt)) { %>| Frontend Standards | `docs/frontend-architecture-standards.md` | `<%= isNx ? 'apps/web' : 'src' %>` |
<% } %><% if (hasFrontend && isAngular) { %>| UI/UX Guidelines | `docs/frontend-ui-ux-guidelines.md` | `<%= isNx ? 'apps/web' : 'src' %>` |
<% } %>| AIM Methodology | `docs/aim-methodology.md` | Everything |
| Session State | `agents/contexts/session-state.md` | Everything |

---

## 2. Agent System

Use the agent alias at the start of your message to activate the corresponding mode:

| Alias | Role | Responsibility |
|-------|------|----------------|
| `@pm` | Project Manager | Coordination, feature decomposition, planning |
<% if (hasBackend) { %>| `@backend` | Backend Agent | <%= isNestjs ? 'NestJS, TypeORM' : isExpress ? 'Express, REST' : 'Next.js API' %>, database |
<% } %><% if (hasFrontend) { %>| `@frontend` | Frontend Agent | <%= isAngular ? 'Angular 21, Signals' : isReact ? 'React 19, hooks' : isNextjs ? 'Next.js, RSC' : isVue ? 'Vue 3, Pinia' : 'Nuxt 3, Vue 3' %>, UI |
<% } %><% if (hasMobile) { %>| `@mobile` | Mobile Agent | <%= isIonic ? 'Ionic, Capacitor, Angular' : 'React Native' %> |
<% } %>| `@security` | Security Agent | Auth, RBAC, data privacy, OWASP |

---
