## Frontend (Vue 3)
- script setup syntax always
- Composables (use* prefix) for reusable logic
- Pinia for global state management
- TypeScript: typed props with defineProps<{...}>()
- Emit: defineEmits<{...}>() — typed
- CSS variables for theming
- Auto-imports via unplugin-auto-import
