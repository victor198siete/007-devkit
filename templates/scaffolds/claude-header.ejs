# CLAUDE.md — AI Agent Instructions for <%= projectName %>

> Auto-loaded by Claude Code at session start.
> Generated by [007devkit](https://github.com/007devkit/007devkit) on <%= date %>

---

## Mandatory Reading Before Generating Code

| Document | Path | Applies to |
|----------|------|------------|
| Transversal rules | `.cursorrules` | Everything |
<% if (hasBackend && isNestjs) { %>| NestJS Standards | `docs/nest-backend-general-standards.md` | `<%= isNx ? 'apps/api' : 'src' %>` |
<% } %><% if (hasFrontend && isAngular) { %>| Frontend Architecture | `docs/frontend-architecture-standards.md` | `<%= isNx ? 'apps/web' : 'src' %>` |
| UI/UX Guidelines | `docs/frontend-ui-ux-guidelines.md` | `<%= isNx ? 'apps/web, apps/mobile' : 'src' %>` |
<% } %><% if (hasFrontend && (isReact || isNextjs)) { %>| Frontend Architecture | `docs/frontend-architecture-standards.md` | `src` |
<% } %><% if (hasFrontend && (isVue || isNuxt)) { %>| Frontend Architecture | `docs/frontend-architecture-standards.md` | `src` |
<% } %>| AIM Methodology | `docs/aim-methodology.md` | Everything |
| Session State | `agents/contexts/session-state.md` | Everything |

---

## Agent System

Use agent alias at start of message to activate mode:

| Alias | Role | Responsibility |
|-------|------|----------------|
| `@pm` | Project Manager | Coordination, decomposition, planning |
<% if (hasBackend) { %>| `@backend` | Backend Agent | <%= isNestjs ? 'NestJS, TypeORM, PostgreSQL, migrations' : isExpress ? 'Express, REST API, middleware' : 'Next.js API Routes, Server Actions' %> |
<% } %><% if (hasFrontend) { %>| `@frontend` | Frontend Agent | <%= isAngular ? 'Angular 21, Signals, Tailwind, RepoHttp' : isReact ? 'React 19, hooks, custom hooks' : isNextjs ? 'Next.js App Router, RSC, Server Actions' : isVue ? 'Vue 3, Composition API, Pinia' : 'Nuxt 3, Vue 3, auto-imports' %> |
<% } %><% if (hasMobile) { %>| `@mobile` | Mobile Agent | <%= isIonic ? 'Ionic, Capacitor, role-based routing' : 'React Native, Expo' %> |
<% } %>| `@security` | Security Agent | Auth, RBAC, tenant isolation, OWASP |

### Available Role Definitions

| Agent | File |
|-------|------|
| Project Manager | `agents/roles/project-manager.md` |
<% if (hasBackend) { %>| Backend | `agents/roles/backend-agent.md` |
<% } %><% if (hasFrontend) { %>| Frontend | `agents/roles/frontend-agent.md` |
<% } %><% if (hasMobile) { %>| Mobile | `agents/roles/mobile-agent.md` |
<% } %>| Security | `agents/roles/security-agent.md` |

---

## Available Workflows

| Command / Task | Workflow |
|----------------|----------|
| Start work session | `agents/workflows/init-session.md` |
<% if (hasBackend && isNestjs) { %>| New NestJS module | `agents/workflows/new-backend-module.md` |
<% } %><% if (hasFrontend) { %>| New component | `agents/workflows/new-frontend-component.md` |
| New feature | `agents/workflows/new-frontend-feature.md` |
<% } %>| Debug an issue | `agents/workflows/debug-issue.md` |
| Write tests | `agents/workflows/write-tests.md` |
| Code review / pre-PR | `agents/workflows/review-code.md` |

---

## Meta-Prompts

| Situation | Template |
|-----------|----------|
| Request new feature | `agents/prompts/feature-request.md` |
| Report and fix a bug | `agents/prompts/bug-report.md` |
| Architecture decision | `agents/prompts/architecture-decision.md` |

---

## Context Management

| Action | File |
|--------|------|
| View current project state | `agents/contexts/session-state.md` |
| Transfer context between sessions | `agents/contexts/handoff-template.md` |

---
