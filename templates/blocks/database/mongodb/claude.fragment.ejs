### MongoDB / Database
- Schema-first approach (Mongoose or Prisma)
- Use sessions for multi-document transactions
- Define indexes explicitly in schema
- Use migration tool for schema changes (never manual in production)
